name: Build
on: [push, pull_request, workflow_dispatch]
permissions: {}

jobs:
  source:
    timeout-minutes: 60
    runs-on: ubuntu-slim

    steps:
    - uses: actions/checkout@v7
      with:
        submodules: 'recursive'

    - name: Build
      run: |
        tar -cvzf cuberite-source.tar.gz *
        sha1sum cuberite-source.tar.gz > cuberite-source.tar.gz.sha1

    - name: Upload artifact
      uses: actions/upload-artifact@v7
      with:
        path: cuberite-source.tar.gz
        archive: false

    - name: Upload artifact
      uses: actions/upload-artifact@v7
      with:
        path: cuberite-source.tar.gz.sha1
        archive: false

  build:
    timeout-minutes: 60
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        target: [linux-x86_64, linux-arm64, macos-x86_64, macos-arm64]
        build_type: [Release, Debug]
        c_compiler: [gcc, clang]
        include:
          - target: linux-x86_64
            os: ubuntu-22.04
          - target: linux-arm64
            os: ubuntu-22.04-arm
          - target: macos-x86_64
            os: macos-15-intel
          - target: macos-arm64
            os: macos-15
          - c_compiler: gcc
            cpp_compiler: g++
          - c_compiler: clang
            cpp_compiler: clang++

    steps:
    - uses: actions/checkout@v7
      with:
        submodules: 'recursive'

    - name: Build
      run: |
        ./nightlybuild.sh -s GitHub -t ${{ matrix.target }} -b ${{ github.ref_name }} -c ${{ matrix.c_compiler }} -cxx ${{ matrix.cpp_compiler }} -m ${{ matrix.build_type }} -n ${{ github.run_number }}

        mv Cuberite.tar.gz cuberite-${{ matrix.target }}.tar.gz
        mv Cuberite.tar.gz.sha1 cuberite-${{ matrix.target }}.tar.gz.sha1

    - name: Upload artifact
      if: matrix.build_type == 'Release' && matrix.c_compiler == 'gcc'
      uses: actions/upload-artifact@v7
      with:
        path: cuberite-${{ matrix.target }}.tar.gz
        archive: false

    - name: Upload artifact
      if: matrix.build_type == 'Release' && matrix.c_compiler == 'gcc'
      uses: actions/upload-artifact@v7
      with:
        path: cuberite-${{ matrix.target }}.tar.gz.sha1
        archive: false

  build-android:
    timeout-minutes: 60
    runs-on: ubuntu-22.04

    steps:
    - uses: actions/checkout@v7
      with:
        submodules: 'recursive'

    - name: Install dependencies
      run: sudo apt install lua5.1

    - name: Build
      run: |
        CMAKE=$ANDROID_SDK_ROOT/cmake/3.18.1/bin/cmake NDK=$ANDROID_NDK android/compile.sh all

        mv android-build/Server/server.zip android-build/Server/cuberite-android-server.zip
        mv android-build/Server/server.zip.sha1 android-build/Server/cuberite-android-server.zip.sha1
        mv android-build/Server/armeabi-v7a.zip android-build/Server/cuberite-android-armeabi-v7a.zip
        mv android-build/Server/armeabi-v7a.zip.sha1 android-build/Server/cuberite-android-armeabi-v7a.zip.sha1
        mv android-build/Server/arm64-v8a.zip android-build/Server/cuberite-android-arm64-v8a.zip
        mv android-build/Server/arm64-v8a.zip.sha1 android-build/Server/cuberite-android-arm64-v8a.zip.sha1
        mv android-build/Server/x86.zip android-build/Server/cuberite-android-x86.zip
        mv android-build/Server/x86.zip.sha1 android-build/Server/cuberite-android-x86.zip.sha1
        mv android-build/Server/x86_64.zip android-build/Server/cuberite-android-x86_64.zip
        mv android-build/Server/x86_64.zip.sha1 android-build/Server/cuberite-android-x86_64.zip.sha1

    - uses: actions/upload-artifact@v7
      with:
        path: android-build/Server/cuberite-android-server.zip
        archive: false

    - uses: actions/upload-artifact@v7
      with:
        path: android-build/Server/cuberite-android-server.zip.sha1
        archive: false

    - uses: actions/upload-artifact@v7
      with:
        path: android-build/Server/cuberite-android-armeabi-v7a.zip
        archive: false

    - uses: actions/upload-artifact@v7
      with:
        path: android-build/Server/cuberite-android-armeabi-v7a.zip.sha1
        archive: false

    - uses: actions/upload-artifact@v7
      with:
        path: android-build/Server/cuberite-android-arm64-v8a.zip
        archive: false

    - uses: actions/upload-artifact@v7
      with:
        path: android-build/Server/cuberite-android-arm64-v8a.zip.sha1
        archive: false

    - uses: actions/upload-artifact@v7
      with:
        path: android-build/Server/cuberite-android-x86.zip
        archive: false

    - uses: actions/upload-artifact@v7
      with:
        path: android-build/Server/cuberite-android-x86.zip.sha1
        archive: false

    - uses: actions/upload-artifact@v7
      with:
        path: android-build/Server/cuberite-android-x86_64.zip
        archive: false

    - uses: actions/upload-artifact@v7
      with:
        path: android-build/Server/cuberite-android-x86_64.zip.sha1
        archive: false

  upload_release:
    # Disable the job until a proper release process is made
    if: (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/experimental') && false
    permissions:
      contents: write
    needs: build-posix
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: release-${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: Automatically uploaded release by GitHub actions
          fail_on_unmatched_files: true
          files: |
                  ${{ github.workspace }}/Cuberite-ubuntu-latest-clang-Release.zip
                  ${{ github.workspace }}/Cuberite-ubuntu-latest-gcc-Release.zip
          prerelease: true
      # Taken from here https://stackoverflow.com/questions/68895194/github-actions-create-or-update-a-git-tag
      - if: ${{ success() }}
        name: Fix tag
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: 'refs/tags/release-${{ github.ref_name }}',
              sha: context.sha
            }).catch(err => {
              if (err.status !== 422) throw err;
              github.rest.git.updateRef({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: 'tags/release-${{ github.ref_name }}',
                sha: context.sha
              });
            })

